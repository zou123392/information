export const state = {
  container: 1250,
  w: 0,
  h: 0,
  sw: 0,
  sh: 0,

  routeArr: ["Login", "Search", "Register", "Forgot", "Home", "Category"], //不需要登录可以访问的页面
};

// function setrouteArr (data) {
//   localStorage.setItem('cartList', JSON.stringify(data))
// }
